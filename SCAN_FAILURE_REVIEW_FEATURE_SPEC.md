# 扫描异常文件集中查看与处理功能开发说明

> **2026-08-23 重构备注**：本说明记录的是初版设计（分页 + 仅“确认损坏”可批量清理）。该模块已于 2026-08-23 重构，以下行为已变更，请以代码与 `CHANGELOG.md` 为准：
> - 错误类型从 4 类扩展为 7 类（网络/权限/文件不存在/容器损坏/资源占用/I-O 错误/未知），支持多选筛选；
> - 列表改为每页 100 条 + 虚拟滚动，支持全选/反选，可一次处理数千条；
> - 新增批量重试（策略 A，云端并发 1）与批量永久删除（所有文件类型均可，后台逐项安全检查、失败跳过）；
> - 批量删除仅一次确认，删除后自动移除资料库记录；支持导出 CSV/JSON。
> 本文件其余内容作为初版设计背景保留。

## 1. 开发目标

在现有“目录扫描异常”机制基础上，新增一个可集中查看和处理扫描失败文件/目录的系统视图。

用户希望在某个资料库文件夹出现三角形感叹号时，可以：

- 快速筛选出该文件夹下所有尚未解决的扫描异常；
- 逐个查看异常文件；
- 尝试播放或打开所在目录；
- 单独重试；
- 标记待删除或永久删除；
- 异常解决后，项目自动从异常列表中消失。

本功能属于“系统状态视图”，不是普通的用户标签功能。

---

## 2. 核心产品原则

### 2.1 不新增普通用户标签

不要在 `videos` 表中新增类似以下字段：

```text
tag = 扫描异常
```

“扫描异常”必须由系统根据未解决的异常记录动态计算：

```text
存在 unresolved scan_failure
→ 显示“扫描异常”

相关 scan_failure 全部 resolved
→ 自动取消“扫描异常”
```

这样可以避免异常已经解决，但标签未清除的状态不同步问题。

### 2.2 用户标签与系统状态分开

用户标签：

- 旅行
- 电影
- 待整理
- 其他由用户手动维护的标签

系统状态：

- 扫描异常
- 元数据失败
- 文件缺失
- 目录读取失败

本次只实现系统状态“扫描异常”视图，不要扩展普通标签系统。

---

## 3. 异常对象分类

扫描异常必须区分三类对象。

### 3.1 已经录入数据库的视频异常

特点：

- 已存在 `VideoRecord`；
- 有视频 ID；
- 可能已经有文件名、路径、封面、收藏、播放历史等数据；
- 但后续扫描、元数据或文件读取失败。

展示方式：

- 复用现有视频卡片或列表项；
- 增加“扫描异常”状态标记；
- 显示错误类型、最近失败时间和重试次数。

可执行操作：

- 播放；
- 查看详情；
- 打开所在文件夹；
- 单项重试；
- 标记待删除；
- 永久删除。

### 3.2 尚未成功录入的视频文件异常

特点：

- 已经发现文件路径；
- 但在读取文件属性、写入数据库或录入阶段失败；
- 可能没有 `videoId`；
- 没有时长、分辨率、封面等完整信息。

展示方式：

使用“异常文件占位卡片”，不要伪装成完整视频卡片。

示例：

```text
⚠ 无法完成录入

文件名：A.mp4
错误：无权访问文件
最近失败：2026-08-02 00:30
重试次数：2
```

可执行操作：

- 打开所在目录；
- 单项重试；
- 永久删除。

安全要求：

- renderer 不得把任意真实路径直接交给删除接口；
- 删除时传递 `failureId`；
- 主进程根据异常记录解析真实路径；
- 主进程重新校验对象仍然存在、仍属于允许的源文件夹范围，再执行删除。

### 3.3 目录异常

特点：

- 目录枚举、权限、离线或超时失败；
- 系统无法确认该目录中到底有哪些视频。

展示方式：

使用“目录异常卡片”。

示例：

```text
⚠ 目录读取失败

目录：临时下载
错误：无法访问该目录
该目录中的文件尚未完成识别
```

可执行操作：

- 打开目录；
- 重新扫描该目录子树；
- 查看错误详情。

不得：

- 虚构目录内具体失败视频；
- 在目录尚未恢复访问前，把里面未知文件显示成视频卡片；
- 直接对整个目录执行永久删除。

---

## 4. 页面与入口设计

### 4.1 左侧导航新增系统视图

在资料库导航中新增：

```text
扫描异常  70
```

建议位置：

```text
所有视频
收藏
待删除
最近播放
扫描异常
重复项
```

数量显示：

- 展示全资料库未解决异常数量；
- 数量随异常解决实时更新。

### 4.2 文件夹三角形感叹号弹窗

现有异常弹窗增加：

```text
失败文件：68
失败目录：2

[查看异常项] [重试异常项]
```

点击“查看异常项”：

- 切换右侧预览区到“扫描异常”视图；
- 自动限定当前源文件夹；
- 不显示其他源文件夹的异常。

建议页面标题：

```text
扫描异常
当前范围：老虎菜
共 70 项
```

### 4.3 全局扫描异常视图

从左侧导航点击“扫描异常”时：

- 默认显示所有启用源文件夹中的未解决异常；
- 支持按源文件夹筛选；
- 支持按异常类型筛选；
- 支持分页。

建议筛选项：

```text
全部异常
视频异常
未录入文件
目录异常
元数据失败
文件读取失败
```

第一期至少支持：

- 全部异常；
- 文件异常；
- 目录异常；
- 当前源文件夹筛选。

---

## 5. 列表项应显示的信息

### 5.1 已录入视频异常

至少显示：

- 视频文件名；
- 所属源文件夹；
- 用户可见路径；
- 异常类型；
- `failureStage`；
- 错误摘要；
- 最近失败时间；
- 重试次数；
- 当前状态；
- 是否缺失；
- 是否已标记待删除。

### 5.2 未录入文件异常

至少显示：

- 文件名；
- 用户可见路径；
- 所属源文件夹；
- `failureStage`；
- 错误摘要；
- 最近失败时间；
- 重试次数。

### 5.3 目录异常

至少显示：

- 目录名；
- 用户可见路径；
- 所属源文件夹；
- 错误摘要；
- 最近失败时间；
- 重试次数；
- 是否离线；
- 是否可访问。

日志中继续使用路径脱敏规则；只有用户自己的 UI 可以展示真实路径。

---

## 6. 数据来源与查询逻辑

### 6.1 以 `scan_failures` 为唯一异常来源

异常列表必须查询：

```text
scan_failures.status = unresolved
或
scan_failures.status = retrying
```

不要依赖单独的前端缓存标签。

### 6.2 与视频记录关联

对于 `objectType = file` 的异常：

1. 使用规范化路径关联 `videos.normalized_path`；
2. 如果能找到视频记录，返回完整视频异常视图模型；
3. 如果找不到视频记录，返回未录入文件异常视图模型。

### 6.3 目录异常

对于 `objectType = directory`：

- 返回目录异常视图模型；
- 不尝试强行关联视频记录。

### 6.4 已解决异常

`status = resolved` 的记录：

- 不出现在默认异常列表；
- 不计入导航数量；
- 可保留在数据库供诊断或历史查看；
- 第一期不需要做“异常历史”页面。

---

## 7. 建议新增的共享类型

具体命名可结合项目现有规范，但建议有明确类型。

```ts
type ScanFailureViewKind =
  | "video"
  | "unindexed-file"
  | "directory";

interface ScanFailureListItem {
  failureId: string;
  sourceFolderId: string;
  kind: ScanFailureViewKind;
  objectType: "file" | "directory";
  displayName: string;
  displayPath: string;
  failureStage: string;
  errorCode: string | null;
  errorSummary: string;
  retryCount: number;
  firstFailedAt: string;
  lastFailedAt: string;
  status: "unresolved" | "retrying";
  video?: VideoRecord;
}
```

查询参数建议：

```ts
interface ScanFailureQuery {
  sourceFolderId?: string;
  kind?: "all" | "file" | "directory";
  failureStage?: string;
  page: number;
  pageSize: number;
}
```

返回结果建议：

```ts
interface ScanFailurePage {
  items: ScanFailureListItem[];
  total: number;
  fileCount: number;
  directoryCount: number;
  page: number;
  pageSize: number;
}
```

---

## 8. 建议新增接口

命名可按项目现有 IPC 规范调整，但语义必须清晰。

```text
listScanFailureItems(query)
getScanFailureCounts(sourceFolderId?)
retryScanFailure(failureId)
retryScanFailures(sourceFolderId)
deleteScanFailureFile(failureId)
openScanFailureLocation(failureId)
```

### 8.1 `listScanFailureItems`

用途：

- 异常页面分页查询；
- 支持全局或单个源文件夹；
- 返回统一视图模型。

### 8.2 `retryScanFailure`

用途：

- 单项重试；
- 只重试一个 failure；
- 文件异常和目录异常分别处理；
- 成功后自动从列表移除。

### 8.3 `deleteScanFailureFile`

用途：

- 删除未录入文件或已录入视频异常对应的磁盘文件；
- 参数必须是 `failureId`；
- 主进程根据数据库记录找到真实路径；
- 重新校验路径和文件状态；
- 删除成功后：
  - 解决该文件的全部未解决异常；
  - 已存在视频记录时按现有产品规则标记 missing 或移除；
  - 刷新 source folder 的异常汇总；
  - 更新页面。

不得允许：

```text
renderer 直接传任意绝对路径进行删除
```

### 8.4 `openScanFailureLocation`

用途：

- 打开文件所在目录或异常目录；
- 只接受 `failureId`；
- 主进程解析路径并调用系统文件管理器。

---

## 9. 单项操作规则

### 9.1 单项重试

文件异常：

- 文件存在：按 `failureStage` 重试必要步骤；
- 文件已确认删除：标记 missing，解决异常；
- 父目录不可读：保留异常；
- EACCES、EPERM 或超时：继续视为真实异常。

目录异常：

- 重新扫描该目录及其子目录；
- 不扩大到整个源文件夹；
- 成功后更新快照并解决目录异常。

### 9.2 单项永久删除

只允许：

- 文件异常；
- 已录入视频异常；
- 未录入文件异常。

目录异常不提供“永久删除目录”。

删除前必须二次确认，至少显示：

```text
将永久删除磁盘上的文件，不进入回收站。
```

删除成功后：

- 解决相关异常；
- 页面自动移除该项；
- 更新导航数量；
- 若当前源文件夹没有剩余异常，三角形感叹号消失。

### 9.3 标记待删除

仅适用于已经存在 `VideoRecord` 的视频异常。

未录入文件异常没有视频记录，不能标记为待删除；只能：

- 打开目录；
- 重试；
- 永久删除。

---

## 10. 自动刷新规则

以下操作完成后，异常页面和文件夹状态必须自动刷新：

- 单项重试成功；
- 批量重试完成；
- 单项删除成功；
- 后台元数据重试成功；
- 正常目录扫描解决异常；
- 文件被确认正常删除；
- 目录恢复访问；
- 新异常产生。

需要刷新：

- 当前异常列表；
- 左侧“扫描异常”数量；
- 文件夹三角形感叹号；
- 异常弹窗数量；
- 当前文件夹视频列表。

不要要求用户手动刷新页面才能看到异常消失。

---

## 11. 边界条件

### 11.1 已删除文件

已经确认正常删除的文件：

- 不继续显示为扫描异常；
- 不允许异常重试再次创建 ENOENT 异常；
- 应标记 missing 并解决相关 failure。

### 11.2 文件恢复出现

此前 missing 的文件重新出现：

- 正常扫描后恢复视频状态；
- 重新处理必要元数据；
- 若处理成功，不进入异常列表。

### 11.3 源文件夹离线

源文件夹整体离线时：

- 保留已有异常；
- 不把所有文件视为已删除；
- 页面可显示目录离线异常；
- 禁止误清理异常记录。

### 11.4 同一路径多个 `failureStage`

一个文件可能同时有：

- file-processing；
- metadata；
- 其他阶段异常。

页面第一期可以：

- 合并成一个卡片；
- 展示主要错误；
- 显示“另有 N 个异常”。

单项重试或删除成功后，应处理该路径下适用的全部未解决异常，不要只解决一个阶段后仍留下虚假警告。

### 11.5 分页与操作后页面为空

例如当前页最后一项被解决：

- 自动回退到上一页；
- 不显示空白死页；
- total 和页码同步更新。

---

## 12. UI 建议

### 12.1 视频异常卡片

```text
[封面]

⚠ 扫描异常
视频名称.mp4
元数据读取失败
最近失败：今天 01:12
已重试：2 次

[播放] [查看原因] [打开目录] [重试] [待删除] [永久删除]
```

### 12.2 未录入文件异常卡片

```text
[异常占位图]

⚠ 无法完成录入
视频名称.mp4
文件属性读取失败
最近失败：今天 01:12
已重试：2 次

[查看原因] [打开目录] [重试] [永久删除]
```

### 12.3 目录异常卡片

```text
[文件夹图标]

⚠ 目录读取失败
临时下载
无法访问该目录，目录内文件尚未完成识别

[查看原因] [打开目录] [重新扫描该目录]
```

---

## 13. 第一阶段实施范围

本次第一阶段必须完成：

1. 左侧新增“扫描异常”系统视图；
2. 显示全资料库异常数量；
3. 支持按源文件夹筛选；
4. 三角感叹号弹窗增加“查看异常项”；
5. 点击后打开当前源文件夹的异常视图；
6. 已录入视频复用视频卡片；
7. 未录入文件使用异常占位卡片；
8. 目录异常使用目录卡片；
9. 支持查看原因；
10. 支持打开所在位置；
11. 支持单项重试；
12. 支持文件永久删除；
13. 已录入视频支持标记待删除；
14. 异常解决后自动从页面移除；
15. 导航数量和三角形感叹号自动更新；
16. 支持分页；
17. 完成 IPC、preload、共享类型、主进程查询和测试。

本次暂不要求：

- 用户自定义标签系统；
- 异常历史页面；
- 批量永久删除全部异常；
- 目录永久删除；
- 按错误码进行复杂高级筛选；
- 重新设计整个视频卡片系统。

---

## 14. 自动化测试要求

至少覆盖以下场景。

### 14.1 查询与分类

1. 已录入视频异常返回 `kind = video`；
2. 未录入文件异常返回 `kind = unindexed-file`；
3. 目录异常返回 `kind = directory`；
4. resolved 异常不出现在默认列表；
5. 支持按 `sourceFolderId` 筛选；
6. 分页总数正确；
7. 文件和目录数量正确。

### 14.2 页面入口

1. 左侧“扫描异常”打开全局异常视图；
2. 文件夹异常弹窗“查看异常项”打开当前文件夹范围；
3. 当前范围标签正确；
4. 返回普通视频视图后筛选状态清理正确。

### 14.3 单项重试

1. 视频异常重试成功后自动移除；
2. 未录入文件重试成功后生成视频记录并移除异常；
3. 目录异常重试成功后更新快照并移除异常；
4. 重试失败后更新错误和 `retryCount`；
5. 同一项重试期间按钮禁用；
6. 不产生重复任务。

### 14.4 删除

1. 删除接口只接受 `failureId`；
2. renderer 不能传任意路径；
3. 主进程校验路径属于源文件夹；
4. 文件不存在时安全处理；
5. 删除成功后解决相关异常；
6. 已录入视频按现有规则更新 missing 或删除状态；
7. 目录异常不显示删除按钮；
8. EACCES 或 EPERM 时保留异常并显示错误。

### 14.5 自动刷新

1. 单项重试后列表、数量和感叹号同步更新；
2. 单项删除后同步更新；
3. 后台元数据成功后异常自动消失；
4. 当前页最后一项移除后页码正确回退。

### 14.6 安全

1. 日志中路径脱敏；
2. 未授权路径不能删除；
3. `failureId` 不存在时安全返回；
4. 已 resolved 的 failure 不允许重复删除；
5. 源文件夹离线时不误判文件已删除。

---

## 15. 验收标准

以下条件全部满足，才算功能完成：

- [ ] 左侧存在“扫描异常”系统视图；
- [ ] 导航显示未解决异常数量；
- [ ] 可查看全资料库异常；
- [ ] 可按当前源文件夹查看异常；
- [ ] 三角形弹窗有“查看异常项”；
- [ ] 已录入视频按视频卡片展示；
- [ ] 未录入文件按异常占位卡片展示；
- [ ] 目录异常按目录卡片展示；
- [ ] 可以查看具体错误原因；
- [ ] 可以打开所在位置；
- [ ] 可以单项重试；
- [ ] 文件异常可以安全永久删除；
- [ ] 已录入视频可以标记待删除；
- [ ] 目录异常不能永久删除目录；
- [ ] renderer 不直接传绝对路径执行删除；
- [ ] 异常成功解决后自动消失；
- [ ] 三角形感叹号和导航数量自动更新；
- [ ] 已确认正常删除的文件不继续显示为异常；
- [ ] resolved 异常不进入默认列表；
- [ ] 支持分页；
- [ ] lint、typecheck、test、build 通过；
- [ ] Electron smoke 和项目现有 E2E 完成；
- [ ] 没有 TODO、空实现或调试代码。

---

## 16. 开发执行要求

请直接基于当前本地工作区开发。

要求：

1. 不要重新拉取或覆盖本地代码；
2. 保留现有未提交修改；
3. 不要修改与本需求无关的播放器、重复项和封面模块；
4. 开始前检查 `git status`；
5. 完成数据库、主进程、IPC、preload、shared types 和 renderer 的完整闭环；
6. 增加自动化测试；
7. 实际运行项目现有质量检查；
8. 按项目 `AGENTS.md` 完成提交和推送；
9. 使用独立功能分支；
10. 建议提交信息：

```text
feat: add scan failure review and filtering view
```

最终报告仅需包含：

```text
当前分支：
Commit：
Push：
Pull Request：
修改文件：
实现功能：
新增接口：
测试结果：
剩余风险：
```

不要只输出设计方案，请完成实际代码、测试、提交和推送。

