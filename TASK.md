# 任务与路线

更新时间：2026-07-11。状态依据代码、测试、历史提交与现有功能审计；“待手测”不等于完成。

> 2026-07-15 更新：已实现源目录安全移除、父/子目录重叠归属和范围外索引清理。仍需在真实 D 盘大目录上验证移除确认计数、耗时和重启后的资料库结果。

> 2026-07-16 网络盘更新：目录发现已改为流式枚举，不再用 30 秒限制整个大目录；现在只在单次目录条目读取连续 30 秒无响应时跳过该分支，且不完整扫描不会清理旧记录。新文件先快速入库，再由单并发后台 FFprobe 队列补齐元数据；任务可跨重启从 `pending` 恢复。仍需真实映射盘验证各超时阈值和长队列耗时。

> 2026-07-17 菜单性能第一轮：已消除扫描状态时间戳造成的无效全页更新，稳定最近播放/封面引用，缓存收藏计数与分页切片，并移除封面路径中的遗留调试请求。真实 2.5 万视频资料库仍需桌面对比验证；数据库级普通列表分页继续列为第二轮根本优化。

> 2026-07-18 菜单性能第二轮：普通资料库已改为 SQLite COUNT + LIMIT/OFFSET 分页，主窗口只接收当前页；目录树、缺失记录、移除影响和播放器队列分别使用轻量/按需接口。只读实库查询100条约 38.5ms、1,259个目录约19ms；真实 Electron 交互仍需验证。

> 2026-07-22 播放清理：已实现播放窗口按钮删除、`Ctrl+D` 二次确认、持久化待删除标记、主页面待删除分页和全库标记清空。renderer 50 项及生产构建通过；新增 SQLite 仓储用例仍被本机 better-sqlite3 ABI 130/137 阻塞，旧库升级与真实文件删除待手测。

> 2026-07-24 缓存安全更新：预览缓存已改为 10 GiB 总上限、分类配额、365 天 TTL 和近似 LRU；生成采用同目录临时文件加原子发布，清理使用 epoch 阻止旧任务重新登记。设置页已显示容量、自动清理状态与失败数。仍需用真实长期资料库验证配额稳定性、低磁盘空间、ACL 拒绝和清理期间关闭应用。

> 2026-07-24 播放器队列与多窗口同步：播放队列已限制为 300 条，并由主进程去重、解析和过滤缺失项；播放器改用固定 URL，通过“先订阅领域事件、后读取带序列快照”同步主窗口与播放窗口。自动测试及打包 smoke 已覆盖边界、竞态和监听器释放；真实双窗口连续收藏、删除、切换及重扫仍需人工验证。

> 2026-07-25 结构化日志与诊断：已增加默认脱敏 JSONL、5 MiB × 5 文件轮转、14 天保留、稳定错误码、operation ID、进程异常记录和设置页诊断预览/导出。诊断包只使用白名单且不上传；真实只读日志目录、文件占用、超大导出和用户主动披露应用目录仍需手测。

> 2026-07-25 发布门禁：固定 Node 22.23.1/npm 10.9.8 下统一 `test:release-gate` 和完整 Node 测试 258/258 通过；Electron ABI 130、unpacked/NSIS、packaged smoke 及首次安装—同包升级—卸载哨兵测试通过。`fast-uri` 已由 3.1.3 升至 3.1.4，`npm audit --omit=dev` 为 0；当前仍因真实物理卷/ACL/锁/磁盘满/SMB、上一正式版本升级、签名和干净 VM 未验证而不得正式发布。

## 已实现（历史自动测试与代码审计有条件支撑）

以下项目已有代码与自动测试支撑。早期 ABI 130/137 阻塞记录仍保留在验证历史中；2026-07-25 已在隔离 Node ABI 127 和 Electron ABI 130 工作目录分别完成验证，完整 Node 测试 258/258。自动测试通过仍不等于真实桌面和物理故障验收。

- P0：Electron/React/TypeScript 脚手架、隔离 preload 与 IPC 契约。
- P0：SQLite 资料库、路径身份规则、收藏、缺失、播放历史。
- P0：发现、元数据、增量扫描、失败持久化、启动同步。
- P0：重命名冲突/非法名/大小写边界保护；永久删除与忘记记录分离。
- P1：资料库多视图、搜索排序、分页、批量渲染、设置页。
- P1：独立播放器、native/mpv 路由、mpv 失败后的系统默认播放器 fallback、控制器、hover 预览、缓存清理。
- P1：开发启动器自动拉起 Vite 并处理进程退出。

## 当前待处理

### P0：发布前验证

- 生产依赖的 `electron-store → conf → ajv → fast-uri` high 传递漏洞已用同主版本补丁关闭，Release workflow 的 `npm audit --omit=dev` 门禁现可通过；全依赖审计仍包含开发工具链风险，需在独立依赖升级任务中评估，禁止直接执行 `npm audit fix --force`。
- 按 `docs/windows-release-checklist.md` 用真实 MP4/MOV/WebM/MKV/AVI/长视频完成 Windows 桌面回归并留证签字。
- 验证 mpv 已安装/未安装、系统默认播放器有/无文件关联、原生解码失败自动回退、中文/空格/超长路径。
- 验证两个真实 NTFS 卷、外部独占文件锁、真实 ACL、物理磁盘满、同名冲突和 SMB/映射盘断线。
- 验证旧库启动后 `is_pending_delete` 幂等补列、标记跨重启保留、播放器连续删除后的队列切换，以及“清空待删除”部分失败时只保留失败项。
- 使用上一正式签名安装包完成跨版本升级，并在无开发工具的干净 Windows VM 验证签名、安装、重启与卸载不删除数据库或源视频。

### P1：可靠性与规模

- 显式 schema version/migration、备份和失败回滚已实现并自动覆盖；后续补真实旧用户库只读副本升级、恢复演练和多个应用实例竞争锁的桌面证据。
- 普通资料库和重复项均已下推 SQLite 分页；后续对超深页码评估 keyset/游标分页，对 `%关键词%` 模糊搜索评估 FTS，并做 10万+ 记录基准。
- 为扫描/缓存加入可取消底层 I/O、完整扫描断点和更细资源上限；后台 FFprobe 已单并发并可恢复，但尚不能中断正在运行的单个进程。量化 1万+ 文件性能。
- 多窗口状态同步基础设施已实现；下一步以真实桌面验证播放器收藏/待删除/永久删除/播放历史、当前视频被外部删除、资料库重扫后的精确刷新，并补充可观测日志。
- 预览缓存的容量、TTL、近似 LRU、手动清理并发和状态展示已自动测试；下一步是在真实长期资料库验证 10 GiB/2 GiB/8 GiB 默认值是否合理，并评估是否开放用户自定义配额或按类型清理。

### P2：体验与可维护性

- 结构化日志与诊断基础设施已实现；后续根据真实支持案例评估错误码覆盖率、日志轮转阈值和是否需要提供“打开日志目录”入口。不得增加自动上传。

- 为结构化日志/诊断导出补真实 Windows 隐私复核、只读/占用/磁盘满场景和异常提示窗口证据。
- 扩展端到端桌面测试与可复用真实媒体夹具。
- 已确认并移除未使用的旧 Electron 远程桥接依赖；后续保持主进程能力仅通过受控 preload/IPC 暴露。
- 评估回收站删除、扫描排除规则、断点续播等产品项；尚未确认，不应直接实现。

## AI 项目经理下一轮审查

1. P0 手测是否有证据而不是口头“通过”。
2. 打包产物在干净 Windows 环境的原生依赖与媒体二进制。
3. SQLite 向后兼容迁移和用户数据恢复方案。
4. 大目录的内存、扫描耗时、FFmpeg 队列与数据库分页。
5. 永久删除产品风险、路径信任边界，以及领域事件在真实双窗口高频操作下的一致性。
