# Windows 发布数据安全验收单

本清单是发布签字记录，不是功能愿望列表。自动化结果来自合成临时数据，绝不能替代真实 NTFS、跨卷、网络盘、签名安装包和升级环境。任一“发布阻断”项未通过时，不得发布。

## 发布候选信息

| 字段 | 填写值 |
| --- | --- |
| 版本/tag | 需要验证 |
| commit SHA | 需要验证 |
| 构建时间（含时区） | 需要验证 |
| Windows 版本/build | 需要验证 |
| CPU/架构 | 需要验证 |
| 文件系统与卷 | 需要验证 |
| Node/npm | 22.23.1 / 10.9.8 |
| Electron/ABI | 33.4.11 / 130 |
| 安装包 SHA-256 | 需要验证 |
| 签名主体/时间戳 | 需要验证 |
| 测试人员 | 需要验证 |

## 自动发布门禁

| 门禁 | 证据要求 | 发布条件 |
| --- | --- | --- |
| `npm ci` | CI run URL 与日志 | 必须通过 |
| `npm run test:release-gate` | lint、build、Windows 文件安全、迁移、性能和完整 Node 测试日志 | 必须全部通过，不允许 continue-on-error |
| `npm run prepare:electron` / `npm run test:electron-smoke` | Electron 版本、ABI、`app.whenReady()` 与临时 SQLite | 必须通过 |
| `npm audit --omit=dev` | 审计报告与处置链接 | 必须通过，或由安全负责人记录有期限的风险接受 |
| `npm run package:dir` / `npm run verify:artifact` | asar 清单和禁止文件检查 | 必须通过 |
| `npm run test:packaged-smoke` | 两阶段启动、CSP/preload、协议、SQLite、媒体工具和数据库重开 | 必须通过 |
| `npm run dist:win` / `npm run release:metadata` | 安装包、校验和、签名分类 | 必须通过 |
| `npm run test:installer-smoke` | 首装、同包升级/修复、运行、卸载、数据库与源视频哨兵 | 必须通过 |

## 真实 Windows 文件安全矩阵

每一行必须填写日期、实际环境、步骤证据和执行人。故障注入通过只证明错误分支，不等同于物理故障通过。

| 场景 | 步骤与预期 | 自动覆盖 | 日期/环境 | 实际结果 | 证据路径或链接 | 执行人签名 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 同卷 NTFS 重命名 | 中文、空格、大小写变化；成功后无占位文件，数据库身份不丢失 | 临时 NTFS + 大小写两阶段测试 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 同卷目标占用 | 外部程序以独占方式持有目标；失败后源文件和数据库不变 | `EBUSY` 故障注入 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| ACL 拒绝 | 普通用户对目标目录无写权限；无临时/占位残留 | `EACCES` 故障注入 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 跨物理卷移动 | 两个 NTFS 卷之间移动；校验后才删除源，数据库失败可补偿 | `EXDEV`、发布失败和回滚注入 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 磁盘满 | 复制中耗尽目标卷；源保留、部分临时文件清理 | `ENOSPC` 故障注入 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 同名同大小不同内容 | 目标不得被大小相同的不同内容覆盖，使用数字后缀 | 合成夹具 + 文件事务测试 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 快速重复清理 | 按缓存大小+时长直接永久删除；产品明确接受内容误判风险，但必须保证每组保留一个且 renderer 不能注入任意路径 | 合成夹具 + IPC/计划篡改测试 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| SMB/映射盘断线 | 扫描中断线不得把既有记录批量标缺失；恢复后可重试 | timeout/offline/子目录失败注入 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |

## 数据库与应用生命周期矩阵

| 场景 | 自动覆盖 | 日期/环境 | 实际结果 | 证据 | 执行人签名 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 新库与 v1–v4 升级 | 每个历史 schema、结构/完整性检查 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| WAL 中存在已提交数据 | `VACUUM INTO` 备份包含 WAL 数据 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 迁移故障与备份失败 | 每一步回滚、原库和备份保留 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 并发启动/SQLite lock | 锁定时拒绝升级且旧 schema 不变 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 首次安装与重启 | 导入、退出、重启后数据库和设置保留 | packaged/installer smoke | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 从上一正式版本升级 | 使用上一签名安装包导入合成资料库，再安装候选版本 | 当前只自动覆盖同包升级/修复 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |
| 卸载 | 应用文件移除，用户数据库和源视频保持逐字节一致 | installer sentinel smoke | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 发布阻断 |

## 性能与体验基线

| 场景 | 自动门槛 | 实机记录 | 状态 |
| --- | --- | --- | --- |
| 10,000 条资料库分页 | 单次 SQLite page query < 10 秒且只返回页大小 | 需要验证 | 发布阻断 |
| 2,000 个同尺寸/同指纹候选 | 单组 listing < 10 秒 | 需要验证 | 发布阻断 |
| 1,000 项缓存维护 | 自动测试限定时间且配额收敛 | 需要验证 | 发布阻断 |
| 300 项播放器队列 | 接受 300、拒绝 301，真实切换无明显冻结 | 需要验证 | 发布阻断 |
| 大型 SMB 资料库 | 扫描可暂停/恢复，UI 可操作，断线不误清 | 需要验证 | 发布阻断 |

## P2 延期风险接受

没有签字的延期不算风险接受。

| 风险编号/描述 | 用户影响 | 缓解措施 | 截止版本/日期 | 接受人签名 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 需要验证 | 需要验证 | 需要验证 | 需要验证 | 需要验证 | 未接受 |

## 最终签字

| 角色 | 姓名/签名 | 日期 | 结论 |
| --- | --- | --- | --- |
| 开发负责人 | 需要验证 | 需要验证 | 不得发布 |
| 测试负责人 | 需要验证 | 需要验证 | 不得发布 |
| 数据安全审查 | 需要验证 | 需要验证 | 不得发布 |
| 发布负责人 | 需要验证 | 需要验证 | 不得发布 |

只有自动门禁全部通过、所有“发布阻断”行有可追溯证据且最终签字均为“批准发布”时，才能改变本表默认结论。
