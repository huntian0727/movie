# 开发与维护报告

## 2026-08-16 取消独立 Web/demo 模式

- 根因：浏览器缺少 preload API，却由 `App.tsx` 用 demo 数据和假成功分支模拟完整资料库，形成与真实 Electron 行为不一致的第二运行模型。
- 修正：应用入口只负责 API 存在性判断；真实 `DesktopApp` 要求注入 API，所有收藏、删除、移动、扫描、重复项、设置和播放操作统一走 preload/IPC。
- React/Vite、Renderer 构建和组件测试继续保留；Vite dev server 仍由 Electron 开发启动器使用。
- 普通浏览器现在只显示 unsupported-runtime，不再掩盖 preload 注入或桌面启动问题。

## 2026-08-16 多 AI 项目记忆与交付追踪

- 建立 `docs/ai/START_HERE.md` 作为首次接手的唯一推荐入口，明确可信度顺序和事实核验方法。
- 新增当前状态、代码定位、风险不变量、关键 ADR 与按次交付模板，避免其他 AI 依赖聊天记录。
- 自动交付要求提交中包含结构化交付记录；它能保证记录存在，但内容真实性仍需代码审查和测试证据验证。
- 本轮仅修改文档与交付脚本，不改变 Electron 业务行为；桌面人工验证不适用。

## 2026-08-01 大目录快照扫描与异常恢复

原有三个 UI 入口最终都落到全目录扫描：左侧刷新扫描一个源目录，异常弹窗仍重新扫描整个源目录，右上角则在 renderer 循环逐个调用同一接口。scanner 每次递归到所有视频并执行文件属性读取；失败只有 `source_folders.scan_error` 摘要，不能精确重试，也不能跨重启呈现明细。

本轮引入 v5 `directory_snapshots`、`scan_failures`、`scan_tasks`，并将接口拆为当前目录扫描、异常重试和全盘顺序扫描。每一级目录独立比较直属名称摘要、计数和 mtime；父目录未变时只跳过其直属视频，仍会检查所有子目录。缺失对账收窄到完整枚举成功的直属目录，父目录确认删除旧子目录后才处理子树。根离线、子目录超时、权限错误和协作取消均不会把不完整结果当成删除依据。

新视频仍快速入库并进入单并发 FFprobe。不同之处是 metadata 失败现在写持久异常；重试只重新调度该阶段，真正成功后才 resolved，并同步清除 source folder 摘要和侧栏感叹号。结构化日志继续只记录 ID、模式和计数，不记录用户完整路径。

合成性能验证覆盖 100 个目录、10,000 视频：未变化重扫读取 101 个目录属性、0 个视频属性；9,900 成功加 100 失败场景中，异常重试读取 0 个目录、只 stat 100 个失败文件。数据库 v1–v6 迁移和仓储测试通过 Electron Node ABI 130 运行；系统默认 Node 24 与项目要求 Node 22.23.1 不一致，标准 `npm test`/执行包 verify 的环境门禁结果必须单独如实记录。

## 2026-07-30 网盘低带宽重复项策略

重复项原流程会对同大小文件读取头/中/尾生成快速指纹，并在批量删除确认前流式读取完整文件计算 SHA-256。对于映射网盘，这两步会产生额外带宽、随机 seek 和长时间等待。

本轮将正式重复组改为 `size_bytes + duration_ms` 精确匹配，且只使用已经保存在 SQLite 的时长。重复项查询不再触发 FFprobe、快速指纹或完整内容哈希；时长尚未成功缓存的文件等待既有后台元数据队列补全，不由重复项页面主动读取。删除确认与执行仍分别复查文件是否存在以及大小、修改时间是否变化，避免扫描后文件被替换。

该策略是有意的带宽/确定性取舍：大小和时长相同不能证明内容相同，因此界面保留显著风险说明、播放入口、保留项选择和永久删除确认。旧指纹 schema 暂不删除，避免为本次行为调整引入数据库迁移风险。

## 2026-07-26 部分视频预览图无法生成

第一处根因位于缓存原子发布流程，而不是视频本身：缓存管理器向生成器传入的临时输出路径以 `.tmp` 结尾，FFmpeg 根据最后扩展名选择图片输出格式时无法识别该路径。旧版本已存在的 JPEG 缓存仍能显示，因此表象是“部分视频”失败；新近访问、重新生成或尚未缓存的视频更容易触发。

临时文件现改为在随机标记之后保留最终 `.jpg` 扩展名，再通过同目录重命名原子发布。该修复不改变缓存键、最终缓存路径、数据库结构或视频文件。已有失败记录会在应用重启后的可见卡片请求，或用户点击“重新生成预览”时重新尝试；无需清理长期缓存。

桌面实机复测随后发现第二处打包专属根因：`ffmpeg-static` 和 `ffprobe-static` 在 ASAR 内返回 `app.asar` 虚拟路径。Electron 的文件 API 可以读取该路径，因此旧冒烟测试误判通过；Windows 进程创建却不能从 ASAR 启动可执行文件。运行时现把确实存在的对应路径解析到 `app.asar.unpacked`，并将打包冒烟检查升级为真实执行两个程序的 `-version`。开发环境路径和 PATH fallback 保持不变。

为避免“待重试”同时被误解为播放、预览或元数据问题，卡片状态现明确显示“元数据失败”或“预览失败”。元数据失败时提供单视频“重新分析”，将记录安全地从 failed 改回 pending 并交给现有单并发 FFprobe 队列；完成后通过领域事件刷新当前页面，不会重新扫描整个目录。

## 2026-07-26 可配置快捷键与设置页精简

设置页不再读取或展示缺失文件列表，避免把扫描产生的技术状态混入常用偏好设置。底层 `is_missing`、扫描 reconcile、普通列表过滤和播放器队列过滤均未删除；本轮只移除设置页入口及 renderer 的无用 `listMissingVideos` 查询。

新增共享快捷键模型，覆盖视频库上一页/下一页，以及播放器播放暂停、快退、快进、音量升降、左右旋转和永久删除。按键使用物理 `KeyboardEvent.code` 与固定修饰键顺序持久化，设置页点击后直接捕获组合；Escape/Enter/Tab 和纯修饰键不可绑定，以保留弹窗、表单和焦点导航。视频库与播放器分别检测重复绑定，IPC 再次校验完整结构和同作用域唯一性；旧用户设置缺少字段时由 `normalizeSettings` 补默认值。

设置保存成功后主进程发布 `settings:changed` 领域事件，已打开的播放器无需重启即可读取新快捷键。相关 6 个测试文件共 70 项通过，TypeScript/Vite production build、unpacked 重打包与包含 `rendererMounted` 的 packaged smoke 通过。尚需人工确认不同键盘布局、输入法、功能键以及用户习惯下的组合是否合理。

## 2026-07-25 Windows 数据安全与发布回归门禁

T12 将此前分散的文件、数据库、Electron 和打包测试收口为 `test:release-gate`，并同时接入 Windows CI 与 release workflow。门禁固定执行 lint、build、文件安全、全部历史 schema 迁移、性能基线和完整 Node 测试；Release 在转换 Electron ABI 前必须先通过该门禁，并执行生产依赖审计，不能用 `continue-on-error` 绕过。

新增的合成资料库在系统临时目录生成同大小不同内容、完全相同内容、同名冲突和由 bundled FFmpeg 实时创建的 32×24 微型 MP4，实际 FFprobe 能读取时长、分辨率和容器。文件矩阵新增跨卷复制中 `ENOSPC` 的部分文件清理；数据库矩阵新增包含已提交 WAL 数据的升级备份和并发写锁拒绝。性能门禁覆盖 10,000 条资料库分页、2,000 个同指纹候选、1,000 项缓存维护和 300/301 播放队列边界。

此前两项 repository 失败均为测试契约错误：第一项没有真的设置收藏，却要求“已收藏”和推荐顺序；修正后同时断言文件名稳定排序及收藏项唯一推荐。第二项把递归父目录统计写成只统计直接子目录；修正为父目录聚合两组、16,000 bytes，保持当前产品定义。未修改仓储业务实现或降低重复删除的完整哈希/文件版本校验。

固定 Node 22.23.1/npm 10.9.8 下，`npm install`、`npm ci`、统一门禁和 258/258 完整测试通过；Electron 33.4.11/ABI 130 smoke、asar 审计、两阶段 packaged smoke、unsigned NSIS 构建和首次安装—同包升级/修复—卸载哨兵测试通过。哨兵证明安装器未修改沙箱用户数据库与源视频。T12 收尾又将生产传递依赖 `fast-uri` 从 3.1.3 补丁升级到 3.1.4；隔离重装后 `npm audit --omit=dev` 为 0，32 个测试文件/258 个测试、3,934 项 asar 审计、packaged smoke 和安装器 smoke 再次通过。

项目目前仍不是正式发布就绪：生产依赖审计的自动阻断已关闭，但真实跨物理卷、NTFS ACL、外部独占锁、物理磁盘满、SMB 断线、上一正式签名版本升级、代码签名与干净 VM 尚未签字。完整开发依赖树仍有历史审计项，涉及测试/构建工具链，必须单独评估升级兼容性，不应使用 `npm audit fix --force` 混入发布修复。完整矩阵见 `docs/windows-release-checklist.md`。

## 2026-07-25 结构化日志、错误分类与诊断导出

此前应用在启动、扫描、缓存和安全边界主要使用零散 `console`，文件操作与数据库失败没有统一 operation ID，无法在用户机器上串联一次操作，也没有容量边界或安全诊断导出。本轮新增主进程 JSONL logger：每条事件包含 timestamp、level、module、operationId、event，并按需包含 durationMs、稳定 errorCode、message 和受限 context。日志默认单文件 5 MiB、最多 5 文件、保留 14 天；批量操作只写开始、计数摘要和失败总数。

路径、URL 密钥、Bearer 凭证、token/password/secret/environment 等敏感字段、Error message/stack 在写盘前同步脱敏。路径只保留本地/网络类别、扩展名和不可逆短哈希。诊断导出使用第二层白名单，仅包含应用/运行时版本、OS/ABI、schema version、SQLite quick_check、日志状态和已脱敏事件；不包含视频、文件名/源路径、数据库文件或数据行、令牌和环境变量值。用户可显式选择披露应用 userData/数据库/缓存/日志目录，但切换后必须重新预览，且该选择不会放宽视频路径边界。

IPC 写操作、扫描、FFprobe、缓存维护、安全拒绝、启动失败、uncaught exception 和 unhandled rejection 已接入。检查现状时发现 `metadataService.ts` 和 `PlayerPage.tsx` 仍有向 `127.0.0.1:7777` 发送视频路径、ffprobe stdout、播放器尺寸和时间轴预览 URL 的遗留调试块；该行为与本地、最小披露边界冲突，已全部移除，不改变 ffprobe 解析、旋转、播放或时间轴失败降级逻辑。定向测试 42/42 通过，补充播放器回归后关联测试 48/48；Node 22 完整测试 250/252，两项仍是 T01 已登记的 repository 断言差异。Electron ABI 130 smoke、3,959 项 asar 制品审计和 packaged smoke 通过。真实只读日志目录、日志文件占用、超大导出和异常提示窗口仍需桌面验证。

## 2026-07-24 有界播放器队列与多窗口一致性

此前主窗口把完整播放队列序列化进播放器 URL，复用窗口时会重新加载页面；队列没有统一上限，主窗口和播放窗口也主要依赖焦点刷新，无法可靠覆盖初始化期间的更新、当前视频删除和监听器生命周期。当前实现把播放会话收回主进程：原始队列最多 300 条，去重并保证选中项存在，再按 SQLite 当前记录过滤未知或磁盘缺失文件；播放器只加载固定 `?player=1` URL，首次创建后切换视频不再整页 reload。

新增主进程领域事件总线，按单调 sequence 广播视频更新/移除、收藏、播放历史和资料库重扫。renderer 先注册监听器、再读取包含同一 sequence 的快照，只补放快照之后的事件，并在窗口卸载时释放精确 listener；业务操作失败时不发布事件。主窗口按当前查询刷新 `LibraryShell`，播放窗口从主进程会话快照重新解析，从而覆盖操作发生在窗口 ready 前后、当前项删除以及重复开关窗口的场景。

Node 22.23.1/npm 10.9.8 隔离副本中，T09 定向测试 69/69 通过，完整测试 234/236；两项失败仍是 T01 已记录的 repository 排序/目录聚合断言差异。lint、build、Electron ABI 130 smoke、unpacked 制品审计和 packaged smoke 均通过。真实双窗口连续收藏、标记待删除、永久删除、快速切换、资料库重扫及窗口关闭重开仍需要人工验证；事件总线是进程内机制，应用完全退出后不会恢复上次播放队列。

## 2026-07-24 有界媒体缓存与清理并发安全

此前“手动清理前一直保留”的缓存策略会使长期使用的封面和时间轴帧无限增长，且协议生成直接写最终文件，清理与在途 FFmpeg 任务并发时可能留下部分图片或让旧任务重新登记。当前实现增加独立 `MediaCacheManager`：默认总量 10 GiB、封面 2 GiB、时间轴 8 GiB、最大年龄 365 天，并用节流后的文件修改时间实现近似 LRU。最低保留规则保护最近 200 张封面和 2,000 张时间轴帧不被单纯 TTL 清理，但容量上限仍优先。

所有新图片先写最终目录内、仅属于本应用的临时文件，非空校验后原子发布。手动清理递增 generation epoch、等待生成和读取退出，只清理 `media-cache/covers` 与 `media-cache/timeline`；旧 epoch 任务不能发布或更新数据库。视频删除、重命名、移动、资料库移除和文件版本变化会触发异步孤儿回收，启动时还会清理由本应用标记的崩溃残留临时文件。设置页现在显示总量、分类用量、上限、最近自动检查和失败数。

Node 22 独立副本中新增缓存/设置测试全部通过，完整测试 224/226；两项失败仍是 T01 已记录的重复项仓储断言，与缓存无关。Electron ABI 130 smoke、unpacked 制品审计和 packaged smoke 通过。尚未用真实 10 GiB 缓存、磁盘满、真实 ACL、杀毒软件占用或映射网盘离线场景手测；自动清理不会访问源视频，但桌面长期运行时的清理耗时仍需采样。

## 2026-07-22 播放清理与待删除工作流

针对高频观看清理场景，播放器增加两类路径：确定不要的视频可由页面按钮或 `Ctrl+D` 打开永久删除确认框，确认后复用主进程现有文件删除链并自动切换相邻视频；暂时不想中断观看的视频可标记“待删除”。该标记写入 SQLite，与收藏相互独立，扫描/upsert 不会清除。

主页面侧栏新增“待删除”分页视图，显示全库有效记录中的标记数量。清空操作针对全部标记记录而不是当前页，确认前显示总数与预计释放空间；逐文件删除允许部分成功，失败记录仍留在数据库并保持标记。主窗口重新获得焦点时会刷新轻量数据，降低独立播放窗口操作后主界面长期陈旧的概率。

本轮 renderer 组件测试 50 项通过，生产构建通过。SQLite 仓储新增了标记持久化、分页和统计测试，但当前 Node 需要 ABI 137，而 better-sqlite3 为 ABI 130，因此 25 个仓储用例在加载原生模块前统一失败；旧用户数据库新增列、真实永久删除、占用文件和网盘失败恢复仍需桌面验证。

## 2026-07-18 左侧菜单性能第二轮

普通资料库从 renderer 全库筛选改为 SQLite 分页：查询返回当前页、总数和校正页码，支持所有视频、收藏、最近播放、目录递归/精确范围、搜索与白名单排序。主窗口不再在启动时调用全量 `listVideos`。目录树只传 DISTINCT directory，缺失记录单独读取，移除目录影响仅在打开确认框时计算，播放器只按当前队列 id（上限300）读取记录。

用户现有数据库只读基准中，25,667条有效记录查询第一页100条连同计数约38.5ms，1,259个去重目录约19ms；旧全量读取曾约185.5ms，且还未包含 Electron IPC 序列化和 renderer 排序成本。新增仓储测试受本机 better-sqlite3 ABI 130/137 阻塞，但分页组件、IPC 契约等43项通过，生产构建通过。真实 Electron 菜单响应、深页 OFFSET 和扫描期间刷新仍需手测。

## 2026-07-17 左侧菜单性能第一轮

当前用户库约有 25,646 条有效视频、1,256 个目录和 1,987 个重复候选组。诊断发现，扫描状态每秒轮询即使只有 `updatedAt` 变化也会替换 React 状态；顶层重渲染又会创建新的最近播放数组和封面函数引用，间接使全库筛选 memo 失效、重置网格封面失败集合。封面加载与 FFmpeg 图片生成路径还残留向本地调试端口发送事件的代码，并在每张图片加载时同步读取布局。

第一轮保持业务和数据库契约不变：只在侧栏可见扫描状态变化时更新状态，复用最近播放数组与封面 URL 函数，缓存收藏计数和当前页切片，并删除遗留调试网络/布局读取。自动测试验证功能不回退，但真实 Electron 菜单响应时间和扫描进行中的体验仍需人工对比；普通列表仍在 renderer 中对全库筛选排序，需由第二轮数据库分页解决。

## 2026-07-16 持久预览缓存

用户要求封面和时间轴预览在未手动清理时长期保留，并与视频管理工具的数据放在一起。原实现使用 `userData/cache`；Windows 文件系统大小写不敏感，实际会与 Electron 的 `Cache` 合并，业务 `covers` 和 Electron `Cache_Data` 混在同一目录，清理边界不清晰。

本轮将业务缓存独立为 `userData/media-cache`。启动时只迁移旧目录的 `covers`、`timeline`，绝不处理 `Cache_Data`；迁移异常只告警，不能阻断应用启动。当时采用“不自动淘汰、源视频变化后旧 key 保留”的长期缓存策略；该历史决策已在 2026-07-24 的 T08 中由有界 LRU/TTL 策略取代。真实已有用户目录的首次迁移、迁移中断恢复仍需桌面手测。

## 演进摘要

2026-07-09 先形成产品设计与分阶段计划，随后建立 Electron/Vite/React/TS 骨架、共享类型和 SQLite 仓储。数据层在首版后连续加固路径不区分大小写、upsert 身份和异常行为；媒体发现/元数据随后补上部分文件、目录失败和扫描错误持久化。

2026-07-10 增加缓存与本地文件操作。重命名经历多轮修复：参数/Windows 名校验、同名目标预留、大小写重命名、失败清理不能误删目标、no-op 处理。之后建立 contextBridge/IPC 与资料库 UI。

2026-07-11 增加独立播放器、设置、启动同步、mpv 回退、时间轴预览；针对播放/预览摩擦做降级。大资料库出现渲染压力后加入分批渲染，再加入前端分页与播放历史。功能审计暴露桌面真实验证不足，最后修复 dev server 自动启动和播放器验证问题。

## 关键问题与影响

- **路径即身份的脆弱性**：重命名会改变 path，但必须保留 video id、收藏和历史。仓储集中处理路径更新，Windows 大小写与唯一约束均需测试。
- **扫描不能因单文件失败全盘失败**：scanner 收集失败、继续导入可读文件，并把摘要写入文件夹 scan_error；根目录不可读则整目录标缺失的语义需谨慎。
- **大网盘不能使用整个发现阶段的固定超时**：原 30 秒 `Promise.race` 会把持续返回数据的大目录误判为离线，且不会取消底层遍历。2026-07-16 改为 `opendir` 流式条目读取，每次无响应才计时；文件随发现处理。子目录读取失败时保留错误但跳过缺失 reconcile，保护未完整枚举的旧用户记录。
- **首次导入不应被 FFprobe 串行阻塞**：扫描现在先写入 path/size/modifiedAt 和 `metadata_status=pending`，目录快速索引完成后再把视频交给单并发后台队列。队列按 id 去重、跨重启恢复，并以 path/size/modifiedAt 条件更新，避免慢结果覆盖重命名或变更后的记录；renderer 轮询 pending 状态，完成前不启动封面 FFmpeg，降低网盘争用。
- **浏览器并非通用视频播放器**：扩展名路由只是近似，实际 codec 仍可能失败，因此运行时先回退 mpv；mpv 启动失败后再由 `shell.openPath` 尝试系统默认播放器。
- **预览生成可能阻塞/抖动**：时间量化减少 hover 请求，主进程串行 FFmpeg 生成防止资源风暴，renderer 记录失败 URL 做占位降级；代价是首帧等待和队列延迟。
- **大列表 UI 压力**：2026-07-18 普通资料库已改为数据库分页，主窗口只持有当前页视频和轻量目录快照；剩余风险转为深页 OFFSET、模糊搜索全表扫描和目录快照随库规模增长。
- **重复项不能沿用全量页面模型**：仅按大小分组时，候选组和 DOM 数量可能很大。2026-07-16 改为进入页面后才查询，SQLite 统计总量并按大小全局排序、按组 LIMIT/OFFSET；renderer 默认只接收 20 组，批量清理明确限制当前页。
- **分页可达性不只包括按钮**：普通资料库新增 30/50 小页、页码直达和左右键翻页；页码提交统一做 1..totalPages 限制，键盘监听排除表单控件、交互按钮与所有弹窗，并在页码/每页数量变化后滚回内容顶部。网格密度使用 CSS column-width 五档调节，偏好只存 renderer localStorage，不进入数据库或 IPC。
- **开发启动不复现**：`dev:electron` 现在主动探测并拉起 Vite、等待可达、联动清理进程；原生模块每次 rebuild 偏慢但降低 ABI 错误。

## 验证边界

自动测试覆盖服务、仓储、IPC 契约与核心 React 组件，但 mock 不证明 Electron 协议流式播放、真实 codec、FFmpeg 二进制、Windows 锁文件、打包安装或 GPU/内存表现。现有 `docs/feature-audit.md` 与 `docs/manual-test-checklist.md` 是下一轮验证依据。

## 2026-07-12 第二轮文档修正

- 确认正式交付根目录为 `.worktrees/codex-video-manager-implementation`，维护文档均纳入该实现分支；顶层 master 仅是历史空壳，不作为本轮交付根。
- 删除外层仓库两个零字节 `console.log(String(...` 异常文件。它们不在 Git 中、无内容，判断为命令转义失误遗留；未删除任何项目资料。
- 以 `ipc.ts` 为准统一播放事实：native → mpv → 系统默认播放器，而非“mpv 失败只报错”。
- 更正 LibraryShell 为分页展示和单项操作，明确批量收藏/删除/移除尚未实现。
- 增加原始设计/当前实现偏差表、数据库改表安全流程和验证结果记录。

仍需人工验证：三级播放 fallback 的窗口和错误体验、真实格式/长视频、Windows 文件锁与权限、大目录资源表现、安装包、旧库迁移演练。当前本机测试还受 `better-sqlite3` ABI 与文件占用阻塞，详见 `docs/verification-results.md`。
# 2026-08-04 重复项后台清理修正

旧流程在打开确认框之前对计划中的所有网盘文件执行 `stat`，确认后又同步等待逐项永久删除，因此按钮反馈、确认框和整个操作节奏都受最慢存储影响。本轮将不可逆操作拆为 SQLite 持久化任务：提交事务只校验数据库计划、写入任务明细并占用 keep/delete 视频，随后立即返回；后台队列串行处理任务并在每次删除前复查受管理路径、文件存在性、大小和修改时间。

安全边界保持不变：保留文件变化会阻断整组；待删文件变化只跳过该项并刷新元数据；只有父目录可读时才把 ENOENT 标记为 missing；磁盘删除成功后才移除视频记录。活动占用还会在其他移动、重命名和永久删除 IPC 入口被拒绝。真实 NAS 响应、应用退出恰逢系统级删除调用等场景仍需桌面手测。
# 2026-08-15 扫描异常安全清理

扫描异常不等于视频损坏：网盘离线、读取超时、权限不足和文件占用都可能令 FFprobe 失败。为了避免把暂时不可访问的视频误删，本轮只将少量明确的容器损坏签名纳入高频清理，其余异常继续保留“重试、播放、打开位置”的人工判断入口。

工作台现在支持对当前页已确认损坏、且已有资料库版本记录的视频进行多选。用户可以先批量加入“待删除”页面集中复核，也可以一次确认后提交永久删除；主进程逐项处理，并在每次删除前复查路径边界、文件类型、大小和修改时间。任一项目发生变化只会失败或跳过该项目，不会扩大删除范围，且只有磁盘文件删除成功后才清理资料库记录。

仍需真实桌面验证：不同网盘供应商的错误文案是否都被正确归入“访问异常”；损坏格式是否存在需要补充、但足够确定且不会混淆为不支持编码的 FFprobe 签名；大量选中项后台处理期间的状态反馈是否符合实际使用节奏。
