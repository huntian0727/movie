# Changelog

提交日期来自 Git 历史；版本尚未正式发布。

## 2026-07-30

### Low-bandwidth duplicate detection

- 重复项识别改为只按 SQLite 中已经缓存的精确文件大小和时长分组；时长未知、读取失败或非正数的记录不进入正式重复组。
- 打开或刷新重复项页面不再调度头/中/尾快速指纹，也不再读取网盘视频内容。
- 批量删除预览不再计算完整 SHA-256；仍会在确认和实际删除前复查文件存在性、大小和修改时间，防止扫描后文件变化。
- 重复项页面统一标注“大小＋时长匹配”及误判风险，并保留播放入口、保留项选择和永久删除确认。
- 旧指纹字段与迁移保留以兼容现有用户数据库，但已退出重复项判断链路。

## 2026-07-26

### Preview cache fix

- 修复原子缓存临时文件以 `.tmp` 结尾，导致 FFmpeg 无法按输出扩展名识别 JPEG 格式、部分新视频预览图生成失败的问题。
- 修复打包程序仍尝试从 `app.asar` 虚拟路径启动 FFmpeg/FFprobe 的问题；运行时现在解析到 `app.asar.unpacked` 中的真实可执行文件。
- 临时文件现在保留最终图片的 `.jpg` 扩展名，同时继续使用随机文件名、原子发布和启动时残留清理机制。
- 增加缓存临时扩展名、打包可执行路径回归测试；打包冒烟测试现在会真实执行 FFmpeg/FFprobe 的 `-version`，不再只检查路径可读。

### Metadata retry experience

- 视频卡片不再使用含义模糊的“待重试”，现在明确区分“元数据失败”和“预览失败”。
- 元数据失败卡片新增“重新分析”按钮，只重试该视频的时长、分辨率和格式读取，不触发整目录扫描。
- 后台分析完成或再次失败后发布视频更新事件，当前页面会自动刷新状态。

### Settings and shortcuts

- 设置页移除“缺失文件”区域；扫描仍会保留缺失标记，普通资料库和播放器仍过滤不可访问文件。
- 设置页列出视频库翻页、播放器播放/暂停、快进/快退、音量、旋转和永久删除共 10 个现有快捷键，支持直接按键重新绑定、单项/全部恢复默认。
- 快捷键按视频库和播放器两个作用域检测冲突，保存在 electron-store；旧设置自动补齐默认值。
- 设置变更通过领域事件同步到已经打开的主窗口和播放器窗口。

### Tests

- 新增快捷键默认值/旧设置归一化、冲突回退、设置页录入、视频库自定义翻页和播放器自定义删除快捷键测试。

## 2026-07-25

### Diagnostics

- 新增默认脱敏的结构化 JSONL 日志：timestamp、level、module、operationId、event、durationMs 和稳定 errorCode。
- 日志按 5 MiB、最多 5 文件轮转并清理超过 14 天的文件；批量操作只记录计数摘要，不逐条记录成功视频。
- 设置页新增诊断预览和 JSON 导出，白名单包含版本、OS/ABI、schema、检查结果和脱敏日志；完整应用目录必须显式选择，视频和数据库正文始终排除。
- 扫描、FFprobe、缓存维护、IPC 写操作、安全拒绝和进程级异常接入统一日志。

### Security

- 移除元数据服务和播放器向本机调试端口发送视频路径、ffprobe stdout、播放器尺寸及预览 URL 的遗留调试代码；应用不会自动上传诊断信息。

### Tests

- 新增路径/凭证脱敏、容量轮转、稳定错误映射、批量日志限量、诊断白名单和设置页预览授权测试。

## 2026-07-24

### Player and window sync

- 播放器原始队列限制为最多 300 条；主进程去重、校验选中项，并过滤数据库未知或磁盘缺失记录。
- 播放器改用固定角色 URL，完整播放会话通过类型化 IPC 快照传递；复用窗口时不再通过完整 query 重新加载页面。
- 新增带单调序列号的主进程领域事件总线，成功操作后同步视频更新/移除、收藏、播放历史和资料库重扫；renderer 采用先订阅后快照的竞态安全初始化并在卸载时释放 listener。

### Cache
- 预览缓存增加 10 GiB 总上限、2 GiB 封面/8 GiB 时间轴分类上限、365 天 TTL、最低近期保留量和节流 `mtime` 的近似 LRU。
- 封面与时间轴帧先写应用专用临时文件，校验非空后原子发布；手动清理使用 generation epoch，生成中清理或退出不会让旧任务重新登记。
- 视频删除、重命名、移动、资料库移除和文件版本变化后异步回收孤儿缓存；启动时清理本应用标记的崩溃残留临时文件。
- 设置页新增缓存总量、分类用量、条目数、自动清理状态、最近检查和失败数；手动清理显示释放空间与失败项数量。
- 自动与手动清理严格限制在 `media-cache/covers` 和 `media-cache/timeline`，不访问或删除源视频，也不触碰 Electron `Cache_Data`。

### Tests
- T09 新增 0/1/300/301 队列边界、去重、选中项缺失、窗口 ready 前后更新、删除当前项、重复开关监听器及跨窗口快照竞态测试；定向 69/69 通过，完整 Node 测试 234/236，剩余两项为已记录的 T01 repository 断言差异。
- 新增超配额 LRU、访问节流、TTL、生成中清理、清理中退出、版本变化、磁盘满/权限失败和 1,000 项性能测试；Node 22 完整测试 224/226，两项为既有 T01 仓储断言差异。

## 2026-07-23

### Fixed
- 播放列表不再把缺失时长强制显示为 `00:00`：已有元数据显示真实时长，当前视频优先使用播放器解码时长，后台分析中的视频显示“分析中”并每 2 秒刷新已加载页，失败或未知时显示 `--:--`。

## 2026-07-22

### Added
- 播放窗口新增永久删除按钮，并支持 `Ctrl+D` 打开同一确认框、`Enter` 确认、`Esc` 取消；删除成功后优先切换到下一条，没有下一条时回到上一条。
- 视频新增持久化“待删除”标记。播放窗口、网格和列表均可标记或取消；主侧栏新增“待删除”分页视图及数量统计。
- “待删除”页面支持一次确认后永久删除数据库中全部标记视频，并显示总数和预计释放空间；部分失败时仅移除成功项，失败项继续保留标记。

### Database
- `videos` 增加带默认值的 `is_pending_delete` 字段和索引；启动时通过幂等列检查兼容旧库。显式 schema version、升级前备份和真实旧库恢复演练仍未实现。

### Tests
- 增加播放窗口按钮删除、`Ctrl+D`、待删除标记、主页面待删除筛选和全部清空交互测试。renderer 50 项通过、生产构建通过；SQLite 仓储测试仍被本机 better-sqlite3 ABI 130/137 不匹配阻塞。

## 2026-07-20

### Duplicate cleanup
- 重复项页新增“优先保留目录”筛选。只返回至少包含选中目录（可选仅当前目录或包含子目录）文件的完整同大小候选组，组内其他目录副本保持可见；每组默认优先保留选中目录中的一个文件，其余候选仍可清理。

## 2026-07-18

### UX
- 新增封面截帧位置设置（0/3/5/10/15 秒，默认 5 秒）；短于目标位置的视频自动截取中间画面，减少开头黑屏导致的无效预览。
- 视频卡片新增单视频“重新生成预览”：仅清除该视频、当前设置位置的封面缓存，并继续走主进程串行图片队列，不触发全库重算。

### Cache
- 封面缓存文件名包含截帧位置。调整设置后旧缓存保留，新位置的封面按需生成；这是降低网盘和大资料库即时负载、以少量额外磁盘占用换取响应速度的取舍。

### Performance
- 普通资料库的所有视频、收藏、最近播放、目录筛选、搜索、排序和30/50/100/200/300分页下推 SQLite；renderer 只接收当前页及总数。
- 主窗口启动不再传输全库：目录树使用去重目录快照，缺失记录使用独立查询，播放器按当前队列 id 读取，目录移除影响改为打开确认框时按需计算。
- 增加目录及资料库常用条件索引；保留原 `library:list` 兼容入口供内部旧调用，不再用于主窗口浏览。

### Tests
- 增加普通资料库分页/导航仓储测试、后端分页组件交互测试和新增 IPC channel 契约；当前 Node ABI 仍阻塞 better-sqlite3 仓储用例实际执行。

## 2026-07-17

### Performance
- 第一轮菜单响应优化：扫描状态轮询在可见状态未变化时复用原数组，稳定最近播放和封面 URL 引用，并缓存收藏计数与当前页切片，减少每秒轮询引发的全库筛选和无效渲染。
- 移除封面加载及 FFmpeg 图片生成路径中遗留的本地调试 HTTP 请求和同步布局读取，避免每页大量封面产生额外主线程与网络开销。

## 2026-07-16

### Cache
- 将封面与时间轴预览从可能和 Electron `Cache` 冲突的旧目录迁移到应用专属 `media-cache`，默认长期保留并仅响应用户手动清理。
- 启动迁移只处理旧 `covers`/`timeline`，保留 Electron `Cache_Data`；迁移失败不会阻止应用启动。

### Added
- 视频网格卡片操作栏在删除按钮旁增加“打开所在文件夹”按钮，复用现有受控 `video:reveal-in-folder` IPC 定位当前视频文件。
- 视频网格卡片增加“只看同目录视频”按钮，点击后清除文件名搜索并按当前视频的直属目录筛选；不会混入子目录视频。

### Tests
- 增加视频卡片打开所在文件夹回调的组件交互覆盖。
- 增加同目录精确筛选以及排除子目录视频的组件测试。

### Fixed
- 修正主界面 Grid 行被视频内容撑出窗口的问题，右侧滚动区域现在可以到达底部分页栏。
- 移除网盘目录发现阶段的全局 30 秒硬超时，改为流式读取目录条目并仅对连续无响应的单次读取计时。
- 子目录读取失败或超时时保留已导入视频且不执行缺失清理，避免不完整扫描误伤旧记录。

### Changed
- 扫描过程改为边发现边处理，侧栏显示动态的“已发现 / 已处理”数量和当前路径。
- 新视频扫描改为先快速写入基础索引，再由单并发后台 FFprobe 队列补齐时长、分辨率和格式；元数据完成前延迟封面生成。

### Added
- 后台元数据任务支持按视频去重、应用重启后恢复 `pending` 记录、失败状态和文件版本条件更新；网格与列表以“分析中/待重试”展示状态。
- 重复项页面增加数据库级分页，默认每页 20 组并支持 10/20/50；提供上一页、下一页和全局重复统计。

### Performance
- 重复项改为进入页面后按需查询，不再随普通资料库刷新加载全部候选；SQLite 增加 `size_bytes` 索引并执行全局大小排序、分页查询。
- 批量清理限定当前页，单项或批量删除后重新查询并自动校正已经超出范围的页码。

### UX
- 重复项批量清理取消输入 `DELETE` 的二次门槛；预检查展示保留数、删除数和预计释放空间后，可直接点击“确认删除”，并继续明确提示永久删除不可撤销。
- 普通视频分页增加每页 30、50 选项，保留 100/200/300 和默认 100；支持输入页码后按 Enter 或失焦跳转，越界自动校正。
- 资料库页面支持键盘左/右键翻页，输入控件、按钮和弹窗激活时不拦截；翻页后自动滚动到内容顶部。
- 网格视图增加 180–400px 五档预览大小控制，瀑布流自动调整列数，并通过 localStorage 恢复上次选择。

## 2026-07-15

### Added
- 源目录支持从资料库移除，并在确认框中预估移除与保留的视频记录数；不会删除磁盘文件。
- 支持父目录与显式子目录重叠配置，新增子目录时已有视频会归属到最具体的源目录。
- 重复项页面支持对单个视频执行带确认的永久删除，完成后自动刷新重复组。
- 重复项支持按文件大小升序或降序排列，同时调整重复组与组内文件顺序。
- 添加目录后立即进入资料库并在后台扫描；手动刷新改为逐目录串行，降低网络盘带宽争用。
- 源目录侧栏显示等待、扫描、暂停、完成、离线和失败状态，并提供暂停、继续与重试操作。

### Fixed
- 移除父源目录时按剩余目录路径并集重新归属视频，避免 SQLite 外键级联误删仍由子目录覆盖的记录。
- 移除目录后立即清理不再属于任何已添加目录的视频索引，后续启动或扫描不会重新加载这些范围外记录。
- 网络盘根目录暂时不可访问时保留已有视频记录，只记录扫描错误；目录枚举、文件访问和 FFprobe 增加超时保护。
- 扫描活动或暂停期间禁止移除源目录，避免后台任务写入已删除的数据库外键。
- 重复项识别改为只按文件大小分组，扫描阶段不再读取文件片段计算内容指纹，显著减少网盘随机访问。

### Tests
- 增加唯一源目录移除、父子目录保留、IPC channel 和 UI 确认流程测试；Node 环境的 SQLite 测试仍受本机 ABI 130/137 不匹配阻塞，Electron ABI 冒烟验证通过。
- 增加重复项大小排序和单项删除确认测试。
- 增加扫描任务状态、暂停/继续和侧栏进度交互测试。

## 2026-07-12

### Docs
- 建立第二轮 AI 可维护化修正：明确正式交付工作树和外层异常空文件处理结果。
- 统一播放策略为 Chromium native → mpv → 系统默认播放器，并保留真实体验待验证标记。
- 更正 LibraryShell 职责，明确批量收藏、删除和移除尚未实现。
- 增加原始设计与当前实现偏差表、数据库安全迁移维护流程及验证结果/手测模板。
- 将测试状态改为条件表述：构建通过，67/86 测试通过，19 项被 better-sqlite3 ABI 阻塞。

## 2026-07-11

### Added
- 原生播放器页、独立复用窗口、播放队列与上下部控制。
- 持久化设置、缓存维护、缺失记录管理、启动同步。
- mpv 回退、进度条 hover 帧、最近播放历史、100/200/300 分页。

### Fixed
- 开发环境可复现启动，`dev:electron` 自动启动并等待 Vite。
- 降低播放与预览失败摩擦，改进失败降级和播放器验证。
- 大资料库先批量渲染，后加入前端分页，降低 renderer 压力。

### Docs/Tests
- 增加功能清单、自查报告和桌面手测计划；播放器、设置、启动同步、路由和历史有自动测试。

## 2026-07-10

### Added
- FFmpeg 封面/时间轴缓存服务和 `local-video://` 媒体入口。
- 本地永久删除、保留扩展名重命名、Electron IPC/preload 桥、资料库 UI。

### Fixed
- 加固缓存默认值和输出目录。
- 连续修复非法文件名、目标冲突、大小写重命名、失败清理误删风险与 no-op 重命名。

## 2026-07-09

### Added
- 产品设计、实施计划及 Electron + React + TypeScript + Vitest 脚手架。
- 共享领域/IPC 类型、SQLite 仓储、视频发现与 FFprobe 元数据、资料库扫描。

### Fixed
- 使脚手架可构建；加固仓储身份规则、媒体元数据边界、扫描容错和根扫描失败持久化。
